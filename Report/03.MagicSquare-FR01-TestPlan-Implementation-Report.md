# Magic Square 4×4 — FR-01 테스트 계획 및 RED 구현 보고서

**문서 유형:** 테스트 계획·구현 현황 (Test Plan & Implementation Report)  
**문서 ID:** RPT-FR01-TP-001  
**버전:** 1.0  
**작성 일자:** 2026-05-29  
**기준 PRD:** 세션 산출 PRD (`docs/PRD_MagicSquare.md` 미저장 — 동등 본문은 `Prompting/02` 참조)  
**관련 문서:** `test_plan.md` (TP-FR01-SIZE-001), `Report/02.MagicSquare-PRD-Review-Report.md`

---

## Executive Summary

Track A(Dual-Track TDD)의 첫 슬라이스로 **FR-01 입력 크기 검증(AC-FR01-01, AC-FR01-05)** 을 앵커로 테스트 계획서를 작성하고, ECB 스캐폴드와 **RED 단계 pytest** 를 구현했다. 본 보고서는 계획 범위, 산출물, PRD 추적성, 현재 테스트 상태를 요약한다.

| 항목 | 상태 |
|------|------|
| 테스트 계획서 | `test_plan.md` (TP-FR01-SIZE-001) |
| 앵커 AC | AC-FR01-01, AC-FR01-05 |
| 구현 계층 | Boundary (`BoundaryValidator`), Control (`MagicSquareControl`) |
| 테스트 | `tests/unit/boundary/test_input_size_validation.py` (7 cases) |
| TDD 단계 | **RED** (구현 최소/스텁 — GREEN 전) |
| 브랜치 | `feature/dual-track-tdd` → `develop` merge 완료 (세션 기록) |

---

## 1. 배경 및 목적

### 1.1 선행 조건

PRD 검토(`Report/02`)에서 FR-01 Boundary 검증과 Domain 격리(AC-FR01-05)가 Dual-Track Track A의 최우선 RED 후보라고 판단되었다. 샘플 예제 선정 기준:

1. FR-01 Acceptance Criteria 중 **가장 선행**인 입력 유효성(크기)
2. `grid = None` → `INVALID_SIZE` 비즈니스 계약 직접 검증
3. Boundary 실패 시 **Domain/Control `resolve` 미호출** 동시 검증 가능

### 1.2 선택 앵커 예제

| 항목 | 값 |
|------|-----|
| AC ID | **AC-FR01-01** (+ 격리 **AC-FR01-05**) |
| FR / BR | FR-01, BR-01 |
| 입력 | `grid = None` |
| 기대 | `code: "INVALID_SIZE"`, `message: "Grid must be 4x4."` |
| PRD 매핑 | §13 `E-BND-001` (코드명은 구현 SSOT `INVALID_SIZE`) |

---

## 2. 테스트 계획 요약 (`test_plan.md`)

### 2.1 In-Scope

- Boundary 계층 4×4 크기 검증 (BR-01)
- P0: `None`, 빈 리스트, 4×0, 3×4, 4×3, 5×5
- AC-FR01-05: `Control.resolve` mock — `assert_not_called`, `call_count == 0`
- 커버리지 목표: Boundary ≥85%, Domain ≥95% (측정: `pytest-cov`)

### 2.2 Out-of-Scope (명시 제외)

| 항목 | 관련 AC |
|------|---------|
| 빈칸 개수 | AC-FR01-02 |
| 값 범위 | AC-FR01-03 |
| 중복 | AC-FR01-04 |
| 4×4 정상 통과 | AC-FR01-06 후보 — **별도 TP** |
| Domain 마방진 로직 | FR-04 (Track B) |
| Solver | FR-05 |

### 2.3 테스트 케이스 ID 체계

| 우선순위 | ID 접두사 | 대표 TC |
|----------|-----------|---------|
| P0 | `UT-BND-SIZE-` | TC-SIZE-001 (`None`) |
| P1 | `UT-BND-SIZE-` | TC-SIZE-002~006 |
| P2 | `UT-BND-ISO-` | resolve spy 전수 |
| 제외 | — | TC-SIZE-007 (4×4 정상) |

---

## 3. 구현 산출물

### 3.1 ECB 스캐폴드

```
src/
  boundary/
    validator.py      # BoundaryValidator.validate()
    errors.py         # BoundaryValidationError
  control/
    magic_square_control.py  # MagicSquareControl.handle()
tests/
  unit/boundary/
    test_input_size_validation.py
pyproject.toml
.gitignore
```

### 3.2 의존 방향

- `MagicSquareControl.handle` → `BoundaryValidator.validate` → (성공 시) `resolve`
- FR-01 실패 경로: `validate` 예외 → `resolve` **호출 금지** (AC-FR01-05)

### 3.3 구현된 테스트 (RED)

| 테스트 함수 | AC | 시나리오 |
|-------------|-----|----------|
| `test_grid_none_raises_invalid_size_error_code` | AC-FR01-01 | `grid=None` |
| `test_grid_empty_list_raises_invalid_size_error_code` | AC-FR01-01 | `grid=[]` |
| `test_grid_four_rows_zero_cols_raises_invalid_size_error_code` | AC-FR01-01 | `[[]]*4` |
| `test_grid_three_by_four_raises_invalid_size_error_code` | AC-FR01-01 | 3×4 |
| `test_grid_none_resolve_called_zero_times_isolation` | AC-FR01-01, 05 | resolve spy |
| `test_grid_none_message_exact_match_prd_section_8_1` | AC-FR01-01 | 메시지 계약 |
| `test_grid_three_by_four_with_invalid_cell_still_invalid_size_only_scope` | AC-FR01-01 | 크기 우선 실패 |

---

## 4. PRD 추적성

| Concept | BR | FR | AC | Test / TC | Component |
|---------|----|----|-----|-----------|-----------|
| 4×4 입력 | BR-01 | FR-01 | AC-FR01-01 | TC-SIZE-001~006 | `BoundaryValidator` |
| Domain 미호출 | — | FR-01 | AC-FR01-05 | resolve mock test | `MagicSquareControl` |
| 오류 코드 | §13 | FR-01 | AC-FR01-01 | `INVALID_SIZE` | `BoundaryValidationError` |

### 4.1 PRD 검토 이슈와의 관계

`Report/02`에서 지적된 항목 중 본 슬라이스가 다루는 것:

- AC-FR01-05 Traceability Matrix 누락 → 테스트로 **직접 검증** 시작
- `Domain resolver` 용어 → `Control.resolve`로 SSOT 고정 (`test_plan.md` §6.1)
- FR-01 성공 경로 AC 부재 → **의도적 제외** (별도 TP)

미해결(본 슬라이스 범위 외):

- AC 전수 Matrix, 두 번째 빈칸 BR, TD-01/02 골든 출력

---

## 5. 오류 계약

| 필드 | 테스트 SSOT | PRD §13 |
|------|-------------|---------|
| `code` | `INVALID_SIZE` | `E-BND-001` |
| `message` | `Grid must be 4x4.` | `Input matrix must be 4x4.` |

> 메시지 문구는 PRD Open Question(DN-03). 본 프로젝트는 **코드 필드**를 assert SSOT로 사용.

---

## 6. TDD 상태 및 다음 단계

### 6.1 현재: RED

- 테스트는 AC-FR01-01/05를 명시
- `BoundaryValidator`는 최소 구현 또는 스텁 — **GREEN 전제로 RED 실패 확인 필요**

### 6.2 권장 다음 작업

1. **GREEN:** `BoundaryValidator.validate` 크기 검사 최소 구현
2. **P1:** TC-SIZE-005 (4×3), TC-SIZE-006 (5×5) 테스트 추가
3. **별도 TP:** AC-FR01-02 (빈칸 개수), AC-FR01-03/04
4. **PRD 개정:** `Report/02` P0 권고 반영 후 `docs/PRD_MagicSquare.md` 저장

---

## 7. 참고 문서·산출물 인덱스

| # | 경로 | 설명 |
|---|------|------|
| 01 | `Report/01.MagicSquare-Problem-Definition-Report.md` | 문제 정의 |
| 02 | `Report/02.MagicSquare-PRD-Review-Report.md` | PRD 7항목 검토 |
| 03 | `Report/03.MagicSquare-FR01-TestPlan-Implementation-Report.md` | 본 문서 |
| — | `test_plan.md` | FR-01 크기 검증 테스트 계획서 |
| — | `Prompting/02.cursor_prd_writing_and_pr_review-transcript.md` | PRD 세션 Transcript |
| — | `Prompting/03.cursor_fr01_test_plan_red_tdd-transcript.md` | FR-01/TDD 세션 Transcript |

---

*End of Report*
